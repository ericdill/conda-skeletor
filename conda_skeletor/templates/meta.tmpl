# NOTE: This script has been automatically generated by github.com/ericdill/conda-skeletor

package:
  name: {{ packagename }}
  version: {{ packageversion }}

source:
  git_url: {{ source_url }}
  git_rev: {{ source_rev }}
{% if patches %}  patches:{% for patch in patches %}
    - {{ patch }}{% endfor %}{% endif %}
build:
  number: {{ build_number }}

{% if build_string %}  string: {{ build_string }}{% endif %}

  # TODO add entry points

requirements:
  build:
    - python{{ python_version_build }}{% for dep in build_requirements | sort %}
    - {{ dep }} # {{ lib_descriptions[dep] }}{% endfor %}
  run:
    - python{{ python_version_build }}{% for dep in run_requirements | sort %}
    - {{ dep }} # {{ lib_descriptions[dep] }}{% endfor %}

test:
  {% if test_requires %}requires:{% for dep in test_requires | sort %}
    - {{ dep }} # {{ lib_descriptions[dep] }}{% endfor %}
{% endif %}
  {% if test_imports %}imports:{% for test_import in test_imports | sort %}
    - {{ test_import }}{% endfor %}
{% endif %}
about:
  home: {{ home_url }}
  license: {{ license }}


# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
# and http://github.com/ericdill/conda-skeletor/readme.rst for more
# information about how this recipe was generated
