# NOTE: This script has been automatically generated by github.com/ericdill/conda-skeletor

package:
  name: {{ packagename }}
  version: {{ packageversion }}

source:
  git_url: {{ source_url }}
  git_rev: {{ source_rev }}

build:
  number: {{ build_number }}
  {% if build_string %}
  string: {{ build_string }}
  {% endif %}
  # TODO add entry points

requirements:
  build:
    - python{{ python_version_build }}{% for build_dep in build_requirements | sort %}
    - {{ build_dep }}{% endfor %}
  run:
    - python{{ python_version_build }}{% for run_dep in run_requirements | sort %}
    - {{ run_dep }}{% endfor %}

test:
  {% if test_requires %}requires:{% for test_require in test_requires | sort %}
    - {{ test_require }}{% endfor %}
  {% endif %}
  {% if test_commands %}commands:{% for test_command in test_commands | sort %}
    - {{ test_command }}{% endfor %}
  {% endif %}
  {% if test_imports %}imports:{% for test_import in test_imports | sort %}
    - {{ test_import }}{% endfor %}
  {% endif %}
about:
  home: {{ home_url }}
  license: {{ license }}


# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
# and http://github.com/ericdill/conda-skeletor/readme.rst for more
# information about how this recipe was generated
